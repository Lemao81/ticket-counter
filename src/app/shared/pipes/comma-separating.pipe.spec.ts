import { CommaSeparatingPipe } from './comma-separating.pipe';

describe('CommaSeparatingPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeparatingPipe();
    expect(pipe).toBeTruthy();
  });
});
