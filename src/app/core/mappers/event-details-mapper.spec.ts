import { EventDetailsMapper } from './event-details-mapper';

describe('EventDetailsMapper', () => {
  it('should create an instance', () => {
    expect(new EventDetailsMapper()).toBeTruthy();
  });
});
